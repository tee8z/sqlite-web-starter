apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-http
  labels:
    {{- include "sqlite-web-starter.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "sqlite-web-starter.selectorLabels" . | nindent 4 }}
